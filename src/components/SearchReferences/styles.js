import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        justifyContent: 'flex-start',
        alignItems: 'center'

    },

    searchInput: {
        backgroundColor: '#fdcfce',
        width: '70%',
        height: 32, 
        borderRadius: 8,
        paddingHorizontal: 8
    }
})

export default styles